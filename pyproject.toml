[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "gal-lib-pyglet"
version = "0.1.0"
description = "基于 pyglet 的现代视觉小说引擎"
requires-python = ">=3.10"
dependencies = [
    "pyglet>=2.1",
]
